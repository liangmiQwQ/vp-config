/**
 * Check whether the config is a workspace root config or a nested config.
 */
export function isWorkspaceRoot(): boolean {
  // 1. Check if there is pnpm-workspace.yaml in the directory, return true if there is.
  // 2. Check the parent directory
  return false
}
